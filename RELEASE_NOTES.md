## v0.1.24 — AI Agent 輸入修正

### Bug Fix
- **IME 組字修正**：Mac 注音（及其他 CJK 輸入法）在 AI 副駕面板選字時，按 Enter / 上下鍵不再誤觸送出訊息。三層防護覆蓋所有平台（macOS WKWebView、Windows WebView2、Linux WebKitGTK）。(#7)

### Enhancement
- **多行輸入**：AI 副駕輸入框支援多行，`Shift+Enter` 換行、`Enter` 送出，自動撐高最多 ~7 行後捲動。

---

⚠ AI 分析僅供參考；自動下單請自行評估風險，盈虧自負。

Shioaji Pro 桌面版 — 內建 shioaji server（sidecar）、伺服器管理介面、系統匣、自動更新。

下載：macOS `.dmg` ｜ Windows `.msi` / `.exe` ｜ Linux `.AppImage` / `.deb` / `.rpm`
